# Leave empty to use the Vite /api proxy in development.
VITE_API_BASE=

# Opt-in demo fixtures when APIs fail or return empty.
# Default is OFF. Never enable in production builds.
# VITE_DEMO_MODE=true
VITE_DEMO_MODE=false
